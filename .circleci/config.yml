version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/install-deps
      - run:
          command: pytest curl2swift/tests
          name: Test

workflows:
  master:
    jobs:
      - build-and-test
